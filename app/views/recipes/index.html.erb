<%= flash[:message] %>
<h1> Welcome to your recipe book! </h1> <br>

<% @recipes.each do |recipe| %>
<tr> <%= link_to(recipe.title, recipe_path(recipe)) %> </tr> <br>
    <%= link_to("Edit Recipe", edit_recipe_path(recipe.id)) %> <br>
    <%= link_to("Delete Recipe", recipe_path(recipe.id), method: 'delete') %> <br> <br>
<% end %>