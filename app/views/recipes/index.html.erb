<%= flash[:message] %>
<h1> Welcome to your recipe book! </h1> <br>

<% @recipes.each do |recipe| %>
<tr> <%= link_to(recipe.title, recipe_path(recipe)) %> </tr> <br>
    <%= link_to "Delete", recipe, method: :delete, data: { confirm: "Really?" } %> <br>
    <br>
<% end %>